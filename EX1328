//1.3.28 Run quick experiments to determine the impact of using
// the termination
//        condition (factor <= n/factor) instead of (factor < n)
//        in Factors in program
//        1.3.9. For each method, find the largest n such that
//        when you type in an n-digit
//        number, the program is sure to finish within 10 seconds.

public class EX1328 {
    public static void main(String[] args)
    { // Print the prime factorization of n.
        long n = 1111111111;//Long.parseLong(args[0]);
        // use System.nanoTime() to calc run time
        long start = System.nanoTime();
        
        for (long factor = 2; factor <= n; factor++)
        { // Test potential factor.
            while (n % factor == 0)
            { // Cast out and print factor.
                n /= factor;
                System.out.print(factor + " ");
            } // Any factor of n must be greater than factor.
        }
        if (n > 1) System.out.print(n);
        long end = System.nanoTime();
        System.out.println();
        // /1000000000 to change nano sec to sec
        System.out.println(1.0*(end-start)/1000000000);
    }
}
