//4.3.9 Add a method peek() to Stack (program 4.3.4) that returns the
// most recently inserted item on the stack (without removing it)
public class EX439 {
    public static void main(String[] args) {
        Stack<Integer> stack = new Stack<>();
        stack.push(1);
        stack.push(11);
        System.out.print(stack.peek());
    }
}

//Program 4.3.4 Generic stack p584
class Stack<Item>
{
    private Node first; // first is top of stk
    private class Node
    {
        Item item;
        Node next;
    }
    public boolean isEmpty()
    { return (first == null); }
    //// first is top of stk
    public void push(Item item)
    { // Insert item onto stack.
        Node oldFirst = first;
        first = new Node();
        first.item = item;
        first.next = oldFirst;
    }
    public Item pop()
    { // Remove and return most recently inserted item.
        Item item = first.item;
        first = first.next;
        return item;
    }
    public Item peek()
    {
        return first.item;
    }

  //  public static void main(String[] args)
  //  {
     //   Stack<String> stack = new Stack<String>();
  //  }
}
